import { ProjectProps } from "../../../contracts/components/Projects/utilityTypes"

export const Project = ({
    title,
    description,
    link,
    image,
}: ProjectProps) => {
    return <div></div>
}