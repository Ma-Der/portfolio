import { ProjectsProps } from "../../contracts/components/Projects";

export const Projects = ({footerHeight}: ProjectsProps) => {
  return <div style={{marginBottom: footerHeight + 30}}>Projects</div>;
};
